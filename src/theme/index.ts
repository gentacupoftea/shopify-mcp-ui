// Import the simplified theme implementation to avoid TypeScript errors
import { createConeaTheme, lightTheme, darkTheme } from './basic-theme';

// Re-export everything
export { createConeaTheme, lightTheme, darkTheme };
export default lightTheme;